<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">

<!-- SqlMapConfig.xml은 데이터베이스 연동 시 반환되는 값을 저장할 빈이나 트랜잭션,
          데이터소스 등 마이바티스 관련 정보를 설정합니다. -->
<!-- src 패키지 아래에 SqlMapConfig.xml은
     mybatis 패키지에(또는 다른명칭의 패키지 사용도 가능함) 작성하며,
     /mybatis/mappers 패키지에 SQL 설정 mapper xml 파일
     (예시 : memeber.xml)을 작성해서 위치시켜 줍니다. -->
<configuration>
   <typeAliases>
   	  <!-- DAO에서 SQL문으로 값을 전달할 때(또는 SQL문을 실행한 후 가져온 값을 DAO로 전달할 때)
   	              사용할 빈을 생성합니다. -->
      <typeAlias type="com.spring.ex04.MemberVO" alias="memberVO"/>
   </typeAliases>

   <environments default="development">
     <environment id="development">
        <transactionManager type="JDBC"/>
        <!-- 데이터베이스 연결을 설정하는 dataSource 정보를 작성합니다. -->
        <!-- https://mybatis.org/mybatis-3/ko/configuration.html -->
        <!-- POOLED - DataSource에 풀링이 적용된 JDBC 커넥션을 위한 구현체이다.
                          이는 새로운 Connection 인스턴스를 생성하기 위해 매번 초기화하는 것을 피하게 해준다.
                          그래서 빠른 응답을 요구하는 웹 애플리케이션에서는 가장 흔히 사용되고 있다. -->
        <dataSource  type="POOLED">
            <property name="driver"  value="oracle.jdbc.driver.OracleDriver" />
            <property  name="url"    value="JDBC:oracle:thin:@localhost:1521:orcl" />
            <property name="username" value="mybatisadd" />
            <property name="password"  value="mybatisadd"/>        
        </dataSource>
     </environment>
   </environments>

	<!-- mappers 엘리먼트를 이용해 마이바티스에서 사용하는 SQL문이 있는
	     XML 파일의 위치를 지정합니다. 여기서, member.xml 파일에는
	          회원 기능 관련 SQL문이 있으며, 이때 반드시 지정한 패키지 이름과 일치해야 함을 주의 바랍니다. -->
	<mappers>
	   <mapper resource="mybatis/mappers/member.xml"/>
	</mappers>
</configuration>
